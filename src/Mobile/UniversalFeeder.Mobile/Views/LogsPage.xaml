<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UniversalFeeder.Mobile.Views.LogsPage"
             Title="Feeding Logs">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Request" Command="{Binding RequestLogsCommand}" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,*" Padding="0">

        <Label Grid.Row="0"
               Text="{Binding Status}"
               HorizontalOptions="Center"
               TextColor="Gray"
               FontSize="13"
               Margin="0,8" />

        <RefreshView Grid.Row="1"
                     Command="{Binding RefreshCommand}"
                     IsRefreshing="{Binding IsRefreshing}">
            <CollectionView ItemsSource="{Binding Logs}"
                            EmptyView="No logs yet. Pull down to refresh.">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="10" Margin="12,4" CornerRadius="8" HasShadow="False">
                            <Label Text="{Binding .}" LineBreakMode="WordWrap" />
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

    </Grid>
</ContentPage>
